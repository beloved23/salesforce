app.controller('SiteController', function($scope, $http) {

    $scope.siteCount = siteCount;
    $scope.siteTimestamp = siteTimestamp;
    $scope.territoryList = territoryList;
    $scope.showSiteList = false;

});